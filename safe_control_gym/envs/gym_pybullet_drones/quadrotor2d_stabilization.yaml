# Composite (system, task) id: 2D quadrotor, stabilization.
#
# A copy of quadrotor.yaml with the system and task axes pinned (quad_type: 2 is
# already that file's default; it is restated here so the id is self-describing
# rather than inheriting the meaning of a shared default). Building this id and
# building `quadrotor` with the same overrides must produce the same environment
# -- tests/test_envs/test_composite_env_ids.py asserts it -- so this file tracks
# its base and must not acquire training-specific values. Put those in
# configs/sb3/ instead.

# Simulation options
ctrl_freq: 60
pyb_freq: 240
physics: pyb
gui: False
quad_type: 2
normalized_rl_action_space: False
episode_len_sec: 5
# State initialization
init_state: null
randomized_init: False
init_state_randomization_info: null
# Randomization
inertial_prop: null
randomized_inertial_prop: False
inertial_prop_randomization_info: null
# Task semantics: stabilization. terminate_on_goal False means entering the goal
# ball does NOT end the episode, so the controller has to hold position there and
# success is judged on the terminal state. The sibling <system>_reach id sets it
# True, ending the episode on first contact.
#
# The base env yamls keep terminate_on_goal True, which is what every shipped
# dataset was collected under -- so the collectors are unaffected by this file.
terminate_on_goal: False

# Task
task: stabilization
task_info: null
cost: rl_reward
# Disturbances
disturbances: null
adversary_disturbance: null
adversary_disturbance_offset: 0.0
adversary_disturbance_scale: 0.01
# Constraints
constraints: null
done_on_violation: False
use_constraint_penalty: False
constraint_penalty: -1
# Misc
verbose: False
# RL Hyper-parameters
norm_act_scale: 0.1
obs_goal_horizon: 0
rew_state_weight: 1.0
rew_act_weight: 0.0001
rew_exponential: True
done_on_out_of_bound: True
